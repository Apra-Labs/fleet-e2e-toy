Scope reviewed: gh-toy-mi2, gh-toy-7rp, gh-toy-4ef (11 issues total across the three feature subtrees). Criteria 1-10 pass: each sprint goal has exactly one feature directly addressing it; every feature (gh-toy-mi2, gh-toy-7rp, gh-toy-4ef) has a downstream [test] task (gh-toy-mi2.3, gh-toy-7rp.2, gh-toy-4ef.2) correctly wired via blocks edges after the impl tasks, not parallel; all task descriptions state concrete acceptance criteria and an explicit '<=3 files' bound; no task exceeds ~3 files; no scope creep or duplicate work observed; no task assumes unbuilt functionality (mi2.2 correctly blocks on mi2.1 for the shared API client/CLI scaffold); all tasks carry model metadata (mi2.1/mi2.2/mi2.3/7rp.1/13t/7rp.2/4ef.2 = standard, 4ef.1 = cheap) satisfying criterion 10.

Criterion 9 (ready-work union) PASSES: gh-toy-mi2.1 is ready now (bd list --parent gh-toy-mi2 --ready --type=task --json returns it), and gh-toy-7rp / gh-toy-4ef both scope-empty on --ready only because their lead tasks (gh-toy-7rp.1, gh-toy-4ef.1) legitimately blocks-depend on gh-toy-mi2.1 (the shared CLI scaffold/entry point built once and reused). bd blocked --parent confirms every blocked chain in all three roots traces back to gh-toy-mi2.1 (ready), not to a self-cycle within its own root. This is the sanctioned cross-goal sequencing case -- no edge should be torn out here.

Criterion 11 FAILS (lane cohesion). Lanes are: s-crud = {gh-toy-mi2.1 (order1), gh-toy-mi2.2 (order2), gh-toy-mi2.3 (order3)}; s-help = {gh-toy-7rp.1 (order1), gh-toy-13t (order2), gh-toy-7rp.2 (order3)}; s-ver = {gh-toy-4ef.1 (order1), gh-toy-4ef.2 (order2)}. All tasks carry streak/streakOrder metadata (no missing-key findings), and each lane is internally cohesive (each centers on one feature's files/component) and under the effort threshold (s-crud effort=60, s-help effort=50, s-ver effort=20, all well under 200 using S=1/M=2/L=4 x standard=10/cheap=1). However there IS a direct 'blocks' edge between an open task in one lane and an open task in a different lane, in two places: (1) gh-toy-7rp.1 (lane s-help) has a 'blocks' dependency on gh-toy-mi2.1 (lane s-crud); (2) gh-toy-4ef.1 (lane s-ver) has a 'blocks' dependency on gh-toy-mi2.1 (lane s-crud). Both gh-toy-7rp.1 and gh-toy-4ef.1 build directly on the CLI entry/arg-parsing file created in gh-toy-mi2.1 (a mutex resource -- only one lane should hold/modify the CLI entry point construction at a time), so per the mutex-resource bullet these should be co-laned with s-crud (or the ordering should be expressed via lane sequencing rather than a raw cross-streak blocks edge). Fix: either (a) re-tag gh-toy-7rp.1, gh-toy-13t, gh-toy-7rp.2, gh-toy-4ef.1, gh-toy-4ef.2 into the same streak as gh-toy-mi2.1 with increasing streakOrder -- e.g. run: bd update gh-toy-7rp.1 --metadata '{"model":"standard","streak":"s-crud","streakOrder":4}'; bd update gh-toy-13t --metadata '{"model":"standard","streak":"s-crud","streakOrder":5}'; bd update gh-toy-7rp.2 --metadata '{"model":"standard","streak":"s-crud","streakOrder":6}'; bd update gh-toy-4ef.1 --metadata '{"model":"cheap","streak":"s-crud","streakOrder":7}'; bd update gh-toy-4ef.2 --metadata '{"model":"standard","streak":"s-crud","streakOrder":8}' -- OR (b) keep three separate lanes but remove the cross-lane blocks edges so gh-toy-mi2.1 is not a cross-streak prerequisite (e.g. duplicate/extract the minimal scaffold piece each lane needs into its own lane, or make the sequencing purely lane-order-based rather than a raw dependency edge). Either fix must preserve the existing ordering semantics (mi2.1 done before 7rp.1/4ef.1 start) without a raw blocks edge spanning two streak ids. No other cross-lane edges found (mi2.2->mi2.1, mi2.3->mi2.2, 7rp.2->13t, 13t->7rp.1, 4ef.2->4ef.1 are all intra-lane).

No dependency-direction bugs found otherwise -- all blocks edges point from later work to earlier prerequisite work as expected, and all parent-child edges correctly point task->feature.
