Scope reviewed: gh-toy-mi2, gh-toy-7rp, gh-toy-4ef (9 open tasks total). Criteria 1,2,4,5(within-feature),9,10 pass. Two concrete problems found:

1. MISSING CROSS-FEATURE ORDERING (criterion 5/7/8 risk): gh-toy-7rp.1, gh-toy-13t, and gh-toy-4ef.1 each state in their description ("COORDINATION" section) that they build on the shared CLI entrypoint (src/cli/index.ts) created by gh-toy-mi2.1, but no `blocks` edge exists from gh-toy-mi2.1 to any of them. `bd ready` currently returns all four (gh-toy-mi2.1, gh-toy-7rp.1, gh-toy-13t, gh-toy-4ef.1) simultaneously, meaning a doer could be dispatched to any of the three dependents before gh-toy-mi2.1 has scaffolded src/cli/index.ts. The task text hedges with "if not yet present, create the minimal entrypoint you need" -- this is a workaround for a missing dependency edge, not a substitute for one, and creates real risk of duplicate/conflicting entrypoint scaffolding across parallel doer runs (multiple agents independently creating src/cli/index.ts). Fix with explicit blocks edges (valid per graph-semantics rules -- these are ordering edges between siblings in different feature subtrees, not parent-child edges, so no deadlock risk):
   bd dep add gh-toy-7rp.1 gh-toy-mi2.1
   bd dep add gh-toy-13t gh-toy-mi2.1
   bd dep add gh-toy-4ef.1 gh-toy-mi2.1

2. ACCEPTANCE CRITERIA CONFLICT (criterion 3): gh-toy-4ef (feature) says --version must print "fleet-e2e-toy v1.0.0", but its child gh-toy-4ef.1 says to read the version from package.json (name="noteapi", version="1.0.0" per repo's actual package.json), which would produce "noteapi v1.0.0" -- a different string. gh-toy-4ef.2's test task ("Assert: stdout contains the version from package.json") will pass only one of the two specs, not both. This is a genuine "what does done look like" ambiguity that should be reconciled before dispatch (either update gh-toy-4ef's feature text to match the package.json-derived string, or update gh-toy-4ef.1/gh-toy-4ef.2 to hardcode "fleet-e2e-toy").

Verified via `bd list --parent <scope> --ready --json` for all three scopes: each returned non-empty (gh-toy-mi2.1 ready under gh-toy-mi2; gh-toy-7rp.1 + gh-toy-13t ready under gh-toy-7rp; gh-toy-4ef.1 ready under gh-toy-4ef) -- no dependency cycles found in any of the three subtrees. All 9 open tasks carry `model` metadata (gh-toy-4ef.1=cheap, rest=standard) -- criterion 10 passes, no fallback needed. Confirmed gh-toy-24g/gh-toy-69s/gh-toy-aqd/gh-toy-s5k are separate parent-less features outside these three subtrees -- correctly excluded from this review, no scope creep found within the reviewed trees. No duplicate-task pairs found beyond the coordination-text risk noted in finding 1. Task sizes are all S/M, none exceed ~3 files.
