Build/lint/test all pass (50/50 tests, clean git status). Reviewed gh-toy-mi2.2, gh-toy-mi2.3, gh-toy-7rp.1, gh-toy-4ef.1.

gh-toy-7rp.1: APPROVED as implemented. src/cli/help.ts + src/cli/index.ts correctly handle global and per-subcommand --help/-h, exit 0, no stack traces; well covered by tests/cli-help.test.ts.

gh-toy-4ef.1: CHANGES NEEDED. Acceptance criteria requires --version/-v to work 'even when combined with other flags,' and the commit message (d2db4ec) explicitly claims this was done ('checked before subcommand dispatch, so it works even when combined with other flags/arguments'). But src/cli/index.ts (lines ~26-29) only checks isVersionFlag against argv[0] (the subcommand slot). Verified directly: run(['--version','list']) correctly prints version and exits 0, but run(['list','--version']) does NOT print the version -- it falls through to listCommand, silently drops the unrecognized --version flag (parseFlags ignores flags not immediately followed usefully), attempts a live API call, and exits 1 with 'Error: Could not reach NoteAPI at http://localhost:3000'. tests/cli-help.test.ts only tests run(['--version']) and run(['-v']) as sole args -- no test exercises --version/-v appearing after a subcommand or combined with other flags, which is the exact scenario the acceptance criteria calls out. Fix: scan the full argv (or at least check the flag both before and after subcommand resolution) for --version/-v before dispatching to a subcommand handler, and add a test for '<subcommand> --version' style invocations.

gh-toy-mi2.2 / gh-toy-mi2.3: Implementation (src/cli/commands/{list,read,create,update,delete}.ts, src/cli/client.ts) matches the described CLI contract and flag handling. Gap: tests/cli-commands.test.ts only tests the 'missing required flag' validation-error path for read/create/update/delete -- no test exercises apiRequest rejecting (e.g. ApiError from a 404) to verify the command actually surfaces a clean non-zero exit for a genuine API failure. gh-toy-mi2.2's own acceptance criterion says 'read with missing/invalid id exits non-zero with a clear error' -- 'invalid id' naturally covers a non-existent id (404), which is untested; only the missing-flag case is covered. This is a real gap, not a nitpick: the sibling file tests/notes.test.ts consistently tests the 404 path for every API endpoint it touches, so this test suite deviates from that established project convention. Recommend adding at least one test per mutating/read command where apiRequest rejects (mockedApiRequest.mockRejectedValueOnce(new ApiError(...))) and asserting the command's promise rejects / propagates so run() exits non-zero with a clean message.

File hygiene: clean. package.json/package-lock.json bin entry addition is justified (wires up the noteapi-cli binary). feedback.md at repo root is leftover plan-review scaffold from an earlier pipeline phase (matches the 'feedback: plan-reviewer-c1-r0' commit predating the dev commits), not introduced by the reviewed tasks -- not flagged as a hygiene issue for this round.

No security issues found (URL params properly encoded via encodeURIComponent and URLSearchParams; no injection vectors).
