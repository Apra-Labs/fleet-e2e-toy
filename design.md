# Design — fleet-e2e-toy

## Problem
- No CLI help, version command, or blank input validation.

## Solution
- Add `--version` and `-v` flag to the CLI.
- Add input validation for blank or empty strings on the add task command.
- Implement help command/options.
