# Local Overrides (not committed to git)

- My test database runs on port 5433, not the default 5432
- I prefer verbose test output: use `npm test -- --verbose`
- When running locally, I use port 3001 to avoid conflicts: `PORT=3001 npm start`
